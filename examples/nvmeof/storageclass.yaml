---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  annotations:
    description: NVMe-oF RWO Filesystem volumes, RWO and RWX Block volumes
  name: csi-nvmeof-sc
allowVolumeExpansion: true
reclaimPolicy: Delete
volumeBindingMode: Immediate
provisioner: nvmeof.csi.ceph.com
parameters:
  csi.storage.k8s.io/controller-expand-secret-name: csi-nvmeof-secret
  csi.storage.k8s.io/controller-expand-secret-namespace: default
  csi.storage.k8s.io/node-stage-secret-name: csi-nvmeof-secret
  csi.storage.k8s.io/node-stage-secret-namespace: default
  csi.storage.k8s.io/controller-publish-secret-name: csi-nvmeof-secret
  csi.storage.k8s.io/controller-publish-secret-namespace: default
  csi.storage.k8s.io/provisioner-secret-name: csi-nvmeof-secret
  csi.storage.k8s.io/provisioner-secret-namespace: default

  # default Ceph-CSI / RBD parameters
  clusterID: <clusterID>
  csi.storage.k8s.io/fstype: ext4
  imageFeatures: "layering"
  pool: mypool

  # subsystemNQN is the NQN of the subsystem where all volumes belong to
  subsystemNQN: "nqn.2025-08.io.ceph:k8s-ceph-csi"

  # connection details for the gateway (management API)
  nvmeofGatewayAddress: 10.242.64.32
  # nvmeofGatewayPort is optional, defaults to 5500
  # nvmeofGatewayPort: "5500"

  # Listener discovery: specify EITHER listeners OR networkMask (XOR),not both.
  #
  # Option A — explicit listener list
  # (static, defined at StorageClass creation time).
  # Note: StorageClass is immutable; if gateways scale up/down, listeners
  # here will be stale.
  # listeners: |
  #   [{
  #     "address": "10.242.64.32",
  #     "port": "4420",
  #     "hostname": "cephnvme-vm9"
  #   }]

  # Option B — networkMask (recommended). CIDR of the cluster
  # network\nvmeof deployment network.
  # The NVMe-oF gateway scans this subnet and automatically creates listeners
  # for every gateway it discovers. The listener list is fetched live at
  # ControllerPublishVolume time, so it stays accurate across gateway
  # scale-up/down.
  networkMask: "10.128.0.0/14"
